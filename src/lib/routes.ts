/** Public marketing site root (playeroneiq.com). */
export const MARKETING_HOME_PATH = "/";

/** Authenticated staff dashboard (post-login home). */
export const STAFF_DASHBOARD_PATH = "/dashboard";
